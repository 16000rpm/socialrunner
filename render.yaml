databases:
  - name: socialrunner-db
    plan: free
    databaseName: socialrunner
    user: socialrunner

services:
  - type: web
    name: socialrunner-api
    plan: free
    runtime: node
    rootDir: backend
    buildCommand: npm ci && npx prisma generate
    startCommand: npx prisma migrate deploy && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: socialrunner-db
          property: connectionString
      - key: FRONTEND_URL
        value: https://socialgirl-app.vercel.app
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
